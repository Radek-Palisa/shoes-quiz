describe('Quiz', () => {
  it.todo('shows loading when data is being fetched');
  it.todo('shows an error screen when data fetching failed');
  it.todo('displays only one question at a time');
  it.todo('uses semantic html');
  it.todo('clicking an answer transitions to the next question');
  it.todo(
    'renders the answer button as type "submit" when being on the last question'
  );
  it.todo(
    'answering the final question fetches the quiz results and shows the results screen'
  );
  it.todo('displays a product card for each result');
  it.todo('clicking on the "restart quiz" button restarts the quiz');
});

// eslint-disable-next-line jest/no-export
export {};
