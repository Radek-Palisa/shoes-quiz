describe('Home', () => {
  it.todo('hovering over the "try quiz" button prefetches the quiz data');
  it.todo('clicking on the "try quiz" routes to the quiz');
});

// eslint-disable-next-line jest/no-export
export {};
